#ifndef UTILS_H
#define UTILS_H

#include <cstdlib>
#include <iostream>
#include <sstream>
#include <string>
#include <time.h>
#include <vector>

std::vector<std::string> splitString(std::string input, char delimeter);
int randomInteger(int min, int max);

#endif
